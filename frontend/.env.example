# Variables del frontend. Vite sólo lee los .env de este directorio, no el de la
# raíz, así que copia este archivo a frontend/.env para sobrescribir los valores.

# Opcional: cae por defecto en http://localhost:4000/api (ver src/services/api.ts).
VITE_API_URL="http://localhost:4000/api"
